apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  namespace: default
spec:
  replicas: 10
  selector:
    matchLabels:
      app: my-deployment
  template:
    metadata:
      labels:
        app: my-deployment
    spec:
      containers:
        - name: my-app-container
          image: ghcr.io/charles1303/kube-argocd-deploy:74b2ae3-36
          imagePullSecrets:
            - name: registry-secret
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
